version: '3.1'

services:

  db:
    image: postgres
    restart: always
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USERNAME: compiledideas
      POSTGRES_PASSWORD: compiledideas@27701
      POSTGRES_DB: crewsec